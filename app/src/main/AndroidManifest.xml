<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.gloria.GameKids">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="ExtraText">
        <activity android:name=".ui.MultiFactorActivity"></activity>
        <activity android:name=".ui.DetailsActivity" />
        <activity android:name=".ui.Vedios" />
        <activity android:name=".ui.vedios" />
        <activity android:name=".BabyVideos" />
        <activity android:name=".ui.play_list" />
        <activity android:name=".ui.signup_details" />
        <activity android:name=".ui.login_details" />
        <activity android:name=".ui.Login" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>